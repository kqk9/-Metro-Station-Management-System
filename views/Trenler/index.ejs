<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Train Management</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="styles/styles.css">
</head>
<body>
    <div class="page-header">
        <div class="container">
            <div class="d-flex justify-content-between align-items-center">
                <h1><i class="fas fa-train me-2"></i>Train Management</h1>
                <a href="/" class="btn btn-back">
                    <i class="fas fa-home me-2"></i>Back to Home
                </a>
            </div>
        </div>
    </div>

    <div class="container pb-5">
        <% if (locals.message) { %>
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="fas fa-check-circle me-2"></i><%= message %>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        <% } %>

        <div class="row mb-4 align-items-center">
            <div class="col-md-6">
                <div class="search-box">
                    <i class="fas fa-search"></i>
                    <input type="text" id="searchInput" class="form-control search-input" placeholder="Search by model...">
                </div>
            </div>
            <div class="col-md-6 text-end">
                <a href="/Trenler/add" class="btn btn-add">
                    <i class="fas fa-plus me-2"></i>Add New Train
                </a>
            </div>
        </div>

        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th><i class="fas fa-hashtag me-2"></i>ID</th>
                        <th><i class="fas fa-train me-2"></i>Model</th>
                        <th><i class="fas fa-building me-2"></i>Brand</th>
                        <th><i class="fas fa-users me-2"></i>Capacity</th>
                        <th><i class="fas fa-cog me-2"></i>Actions</th>
                    </tr>
                </thead>
                <tbody id="personalTableBody">
                    <% Trenler.forEach(tren => { %>
                        <tr>
                            <td><%= tren.TrenID %></td>
                            <td><%= tren.Model %></td>
                            <td><%= tren.Marka %></td>
                            <td><%= tren.Kapasite %></td>
                            <td class="action-buttons">
                                <a href="/Trenler/edit/<%= tren.TrenID %>" class="btn btn-edit">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <form action="/Trenler/delete/<%= tren.TrenID %>" method="POST" class="d-inline">
                                    <button type="submit" class="btn btn-delete" onclick="return confirm('Are you sure you want to delete this train?')">
                                        <i class="fas fa-trash-alt"></i>
                                    </button>
                                </form>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
   <script src="js/Trenler.js"></script>
</body>
</html>